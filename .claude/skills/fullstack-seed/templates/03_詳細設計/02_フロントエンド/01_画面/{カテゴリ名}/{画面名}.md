---
spec_runner:
  node_id: 詳細.画面.{画面名}
  kind: detailed_design
  depends_on:
    - 概要.フロントエンド.画面一覧
    - 概要.フロントエンド.画面遷移図
    - 詳細.コンポーネント.{コンポーネント名}
  maps_to:
    - src/app/{path}/page.tsx
    - tests/app/{path}/
---

```yaml
概要:
  title: {画面名}（SCR-{番号} /{path}）
  purpose: {この画面が達成することを1文}
  satisfies: [REQ-{XX}]

入出力:
  inputs:
    - name: {URLパラメータ / フォーム項目 / 取得API名}
      type: {整数 / 数値 / 文字列 / 真偽 / 日時 / パス / 列挙(a|b)}
      rule: {バリデーション制約。例: 1〜100 の整数、必須}
      desc: {説明。APIなら呼び出しタイミング}
  outputs:
    - name: {表示要素 / 送信API / 遷移}
      dest: {遷移先画面・送信先エンドポイント}
      desc: {説明}
  exceptions:
    - type: {エラー表示・エラー状態}
      cond: {API失敗・バリデーション違反など。1原因1エントリ}

フロー:
  - step: 1
    do: {表示時の処理（データ取得・初期表示）。使用コンポーネントは CMP-{番号} で参照する}
    error: {発生条件} -> {エラー表示}
  - step: 2
    on: {トリガー操作。例: 保存ボタン押下}
    do: {入力を rule に従い検証し、{処理}を行う}
    error: {バリデーション違反} -> {エラー表示}
    next: {遷移先画面 / なし}

テスト仕様:
  - id: T-01
    type: 結合
    case: {正常表示・主要操作のケース}
  - id: T-02
    type: 結合
    case: {バリデーション違反・API失敗時のケース}
    covers: [exceptions.{発生条件}]

補足:
  レイアウト: |
    +---------------------------+
    | {ヘッダー}                |
    +---------------------------+
    | {メインコンテンツ}        |
    +---------------------------+
    | {フッター}                |
    +---------------------------+
```
