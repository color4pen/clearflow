---
spec_runner:
  node_id: 詳細.コンポーネント.{コンポーネント名}
  kind: detailed_design
  depends_on:
    - 概要.フロントエンド.コンポーネント構成
  maps_to:
    - src/components/{カテゴリ名}/{コンポーネント名}/
    - tests/components/{カテゴリ名}/{コンポーネント名}/
---

```yaml
概要:
  title: {コンポーネント名}（CMP-{番号}）
  purpose: {このコンポーネントが提供する UI・機能を1文}

入出力:
  inputs:
    - name: {prop名}
      type: {整数 / 数値 / 文字列 / 真偽 / 日時 / パス / 列挙(a|b)}
      default: {省略可。必須なら書かない}
      rule: {制約。なければ省略}
      desc: {説明}
  outputs:
    - name: {イベント名}
      desc: {発火条件とコールバック引数}
  exceptions:
    - type: {エラー表示・フォールバック表示}
      cond: {発生条件。1原因1エントリ}

状態:
  - 表示パターン: {パターン名}
    条件: {このパターンになる条件}
    表示: {表示内容}

テスト仕様:
  - id: T-01
    type: 単体
    case: {props に応じた表示パターンのケース}
  - id: T-02
    type: 単体
    case: {イベント発火のケース}
```
