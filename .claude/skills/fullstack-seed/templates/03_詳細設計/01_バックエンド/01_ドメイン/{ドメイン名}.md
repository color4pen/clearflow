---
spec_runner:
  node_id: 詳細.ドメイン.{ドメイン名}
  kind: detailed_design
  depends_on:
    - 概要.バックエンド.ドメインモデル
  maps_to:
    - src/domain/{ドメイン名}/
    - tests/domain/{ドメイン名}/
---

```yaml
概要:
  title: {ドメイン名}
  purpose: {このドメインが扱うビジネス概念を1文}

定数:
  {制約値名}: {値}   # {なぜこの値か。例: 注文明細の上限数}

入出力:
  inputs:
    - name: {公開操作名}
      type: 操作
      desc: {引数 -> 戻り値。事前条件があれば書く}
  exceptions:
    - type: {ドメイン例外型}
      cond: {不変条件違反の条件。1原因1エントリ}

状態:
  - 集約: {集約名}
    ルート: {ルートエンティティ}
    含む: [{エンティティ}, {値オブジェクト}]
    不変条件:
      - {常に成り立つルール}
  - 値オブジェクト: {名前}
    制約: {制約・ルール}

テスト仕様:
  - id: T-01
    type: 単体
    case: {不変条件が保たれるケース}
  - id: T-02
    type: 単体
    case: {不変条件違反で例外が送出されるケース}
    covers: [exceptions.{発生条件}]
```
