---
spec_runner:
  node_id: 詳細.外部サービス.{サービス名}
  kind: detailed_design
  depends_on:
    - 概要.システム俯瞰図
  maps_to:
    - src/infrastructure/{プロバイダー名}/{サービス名}/
    - tests/infrastructure/{プロバイダー名}/{サービス名}/
---

```yaml
概要:
  title: {サービス名}
  purpose: {このサービスを何の目的で使うか1文}

定数:
  ENDPOINT: "{エンドポイント / リージョン}"   # {選定理由}
  TIMEOUT_MS: {値}                            # {なぜこの値か}
  RETRY_MAX: {値}                             # {なぜこの回数か}

入出力:
  inputs:
    - name: {操作名}
      type: 操作
      desc: {引数 -> 戻り値。使用する UC}
  exceptions:
    - type: {例外型}
      cond: {タイムアウト・認証失敗など。1原因1エントリ}

フロー:
  - step: 1
    do: {接続・認証の手順。設定は ENDPOINT など定数名で参照する}
    error: {発生条件} -> {例外型}
  - step: 2
    do: {失敗時は RETRY_MAX 回までリトライし、TIMEOUT_MS 超過時のフォールバック}

テスト仕様:
  - id: T-01
    type: 結合
    case: {主要操作が成功するケース}
  - id: T-02
    type: 結合
    case: {タイムアウト時に例外が送出されるケース}
    covers: [exceptions.{発生条件}]
```
