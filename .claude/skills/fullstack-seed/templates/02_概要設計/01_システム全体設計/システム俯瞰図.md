---
spec_runner:
  node_id: 概要.システム俯瞰図
  kind: overview_design
  depends_on:
    - 要件.要件定義
  maps_to:
    - docs/02_概要設計/01_システム全体設計/
---

# システム俯瞰図

フロントエンド・バックエンド・外部サービスの関係を示す。

## コンポーネント全体図

```mermaid
graph LR
  User([ユーザー])
  subgraph フロントエンド
    {UI}
  end
  subgraph バックエンド
    {API}
    {ドメイン}
  end
  Ext([{外部サービス}])

  User --> {UI}
  {UI} --> {API}
  {API} --> {ドメイン}
  {API} --> Ext
```

## 外部インターフェース

| 外部システム | 方向 | プロトコル | 概要 |
|------------|------|----------|------|
| {外部システム名} | 受信 / 送信 | {HTTP / gRPC など} | {概要} |
